language: swift
os: osx
osx_image: xcode10.2

skip_install: true

script:
  - swift test --enable-code-coverage

after_success:
  - bash <(curl -s https://codecov.io/bash) -f .build/debug/codecov/Scout.json
